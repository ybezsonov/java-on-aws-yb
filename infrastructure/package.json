{
  "scripts": {
    "generate-cfn": "cd ./cdk && cdk synth workshop-infrastructure --yaml --path-metadata false --version-reporting false | sed 's/arn:aws:iam::{{.AccountId}}:/!Sub arn:aws:iam::${AWS::AccountId}:/g' > ../cfn/workshop-infrastructure.yaml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yaml": "^2.5.1"
  }
}
